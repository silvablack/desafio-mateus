### Run application ###
+   Clone this repository:
        `git clone`
+   Install modules:
        `npm install`
+   Start Application:
        `npm start`


### Tests ###
+   Run command line:
   + run test unit
        `npm run test-unit`

### Run docker ###
+   Run command line:
   + run docker file
        `docker build .`